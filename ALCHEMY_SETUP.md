# 🔑 วิธีรับ Alchemy API Key (ฟรี!)

เพื่อให้แอปดึงข้อมูล transaction จริงจาก Base network คุณต้องมี Alchemy API Key

## ขั้นตอนการรับ API Key

### 1. สมัครบัญชี Alchemy
1. ไปที่ https://www.alchemy.com/
2. คลิก **"Sign Up"**
3. กรอกข้อมูล:
   - Email
   - Password
4. Verify email (เช็คกล่องจดหมาย)

### 2. สร้าง App และรับ API Key
1. Login เข้า Alchemy Dashboard
2. คลิก **"+ Create new app"**
3. กรอกข้อมูล:
   - **Name**: เช่น `Airdrop Checker`
   - **Chain**: เลือก **Base**
   - **Network**: เลือก **Base Mainnet**
4. คลิก **"Create app"**
5. คลิกที่ app ที่สร้าง → คลิก **"API Key"**
6. คัดลอก API Key

### 3. เพิ่ม API Key ลงในโปรเจกต์
แก้ไขไฟล์ `.env.local`:

```bash
ALCHEMY_API_KEY=your_actual_alchemy_api_key_here
```

### 4. Restart Dev Server
```bash
# หยุด server เดิม (Ctrl+C)
npm run dev
```

## ✅ ข้อดีของการใช้ Alchemy API

### ก่อนใช้ API (ข้อมูลประมาณการ):
- ❌ First Transaction: ประมาณการจาก tx count
- ❌ Active Days: คำนวณจาก tx / 2
- ⚠️ ไม่แม่นยำ

### หลังใช้ API (ข้อมูลจริง):
- ✅ First Transaction: **วันที่จริงที่ทำ tx แรก**
- ✅ Active Days: **นับจากวันที่ unique จริง**
- ✅ Transaction Count: **จำนวน tx ทั้งหมดที่แท้จริง**
- ⚡ **เร็วกว่า**: ดึงข้อมูลได้รวดเร็วภายใน 1-3 วินาที

## 🆓 ข้อมูล API Limits (Free Tier)

Alchemy Free Tier มี limit:
- **300M compute units/month**
- **5 requests/second**

เพียงพอสำหรับใช้งานทั่วไป! (ประมาณ 100,000+ requests/month)

## 🧪 ทดสอบว่า API ทำงาน

1. รัน dev server: `npm run dev`
2. เปิด http://localhost:3000
3. กรอก wallet address: `0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb`
4. ตรวจสอบ:
   - ไม่มี badge \"Estimated\" → ใช้ข้อมูลจริงแล้ว ✅
   - มี badge \"Estimated\" → ยังใช้ข้อมูลประมาณการ ⚠️
   - โหลดเร็ว (1-3 วินาที) → Alchemy ทำงานปกติ ⚡

## 🔒 ความปลอดภัย

- ✅ API Key เก็บใน `.env.local` (ไม่ขึ้น git)
- ✅ เป็น **read-only** API (อ่านข้อมูลเท่านั้น)
- ✅ ไม่มีความเสี่ยงต่อเงินหรือ assets

## 🌟 ทำไมต้อง Alchemy?

Alchemy เป็น infrastructure provider ชั้นนำที่ใช้โดย:
- OpenSea
- Dapper Labs
- Adobe
- และอีกหลายบริษัทใหญ่

มี API ที่:
- **เร็วกว่า** RPC ทั่วไป
- **เสถียร** และมี uptime สูง
- **Enhanced APIs** สำหรับดึงข้อมูลซับซ้อน

## ⚠️ หมายเหตุ

ถ้าไม่ได้ใส่ Alchemy API Key:
- แอปจะใช้วิธี **fallback** (ดึงจาก RPC โดยตรง)
- ข้อมูลจะเป็นการ **ประมาณการ** เท่านั้น
- จะมี badge **"Estimated"** แสดงในหน้าผลลัพธ์

---

**แนะนำ:** ควรใช้ Alchemy API เพื่อความแม่นยำและความเร็วสูงสุด! 🎯⚡
